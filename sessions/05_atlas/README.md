# Day 5: ATLAS-only session

Install `kernel.json` for respective conda environment so we can use it in a jupyter notebook:
```
source setup.sh
```

Datasets are available in
```
/pscratch/sd/d/dnoll/projects/ml4fp/atlas
```